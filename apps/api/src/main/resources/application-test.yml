spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: sa

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  flyway:
    enabled: false

file:
  upload-dir: target/test-uploads
  private-dir: target/test-private

security:
  jwt:
    secret-key: ThisIsASecretKeyForIntegrationTestingOnly!12345
